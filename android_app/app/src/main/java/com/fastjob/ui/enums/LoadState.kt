package com.fastjob.ui.enums

/**
 * Enum que representa el estado de carga de un recurso.
 */
enum class LoadState {
    START,
    LOADING,
    LOADED,
    ERROR,
    NOT_FOUND,
    END_OF_LIST
}
